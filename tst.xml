
<properties>
<property name="try" type="string">
<![CDATA[		try {
		} catch(COMException) {
			Console.WriteLine("no Excel found.");
		}

]]>
</property>
<property name="cs" type="string">
<![CDATA[/*
*/
using System;
using System.IO;
using System.Text;
using System.Linq;
using System.Windows.Forms;
using System.Collections.Generic;

namespace MyNameSpace {
class Program
{
	static void Main() {
		string fp = "xtool.csv";
	}
} // main class
} // namespace
]]>
</property>
<property name="dev" type="string">
<![CDATA[
Get-Process -name sqldeveloper64W | %{ $_.MainWindowTitle}
]]>
</property>
<property name="tmp2" type="string">
<![CDATA[
Get-Process -name sqldeveloper64W | %{ $_.MainWindowTitle}
]]>
</property>
<property name="tmp1" type="string">
<![CDATA[
Get-Process -name sqldeveloper64W | %{ $_.MainWindowTitle}
]]>
</property>
<property name="@@0" type="string">
<![CDATA[
■■テレワーク開始報告（尹）■■
■テレワーク連絡事項
　テレワークを開始します。
■本日の作業予定
　・障害対応@0@$$
■終了予定時刻
　18:00
]]>
</property>
<property name="@@1" type="string">
<![CDATA[
■■テレワーク終了報告（尹）■■
■テレワーク連絡事項
　テレワークを終了します。（@time@）
■本日の作業実績
　・障害対応@0@$$
]]>
</property>
<property name="func" type="string">
<![CDATA[function(n){
	$$com.Trace(Editor, n.stripEnd('"'));
}
]]>
</property>
<property name="csform" type="string">
<![CDATA[
/*
*/
using System;
using System.IO;
using System.Text;
using System.Linq;
using System.Windows.Forms;
using System.Collections.Generic;
using System.Runtime.InteropServices;

public class MainForm: Form
{
	[DllImport("kernel32.dll")]
	public static extern bool AllocConsole();
	
	public MainForm() {
		AllocConsole();
		
		this.AllowDrop = true;
		this.DragEnter += MF_DragEnter;
		this.DragDrop += MF_DragDrop;
	}
	
	private void MF_DragEnter(object sender, DragEventArgs e) {
		if (e.Data.GetDataPresent(DataFormats.FileDrop)) {
			e.Effect = DragDropEffects.Copy;
		} else {
			e.Effect = DragDropEffects.None;
		}
	}
	
	private void MF_DragDrop(object sender, DragEventArgs e) {
		if (e.Data.GetDataPresent(DataFormats.FileDrop)) {
			string[] paths = (string[])e.Data.GetData(DataFormats.FileDrop);
			foreach (string path in paths) {
				if (File.Exists(path)) {
					Console.WriteLine("file:{0}", path);
				} 
				else if (Directory.Exists(path)) {
					Console.WriteLine("dir:{0}", path);
				}
			}
		}
	}
	
	[STAThread]
	public static void Main()
	{
		Application.EnableVisualStyles();
		Application.SetCompatibleTextRenderingDefault(false);
		Application.Run(new MainForm());
	}

}
]]>
</property>
</properties>
